# Pour Qui Voter?

## How to run locally

```
$> git clone git@github.com:xdamman/pourquivoter.git pourquivoter
$> cd pourquivoter
$> npm install
$> npm run dev
info: > Ready on http://localhost:3000
```
